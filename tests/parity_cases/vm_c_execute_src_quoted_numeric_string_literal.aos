Program {
  Export(name=start)

  Let(name=start) {
    Fn(params=_) {
      Block {
        Return {
          Call(target=sys.str.find) {
            Lit(value="76103")
            Lit(value="7")
            Lit(value=0)
          }
        }
      }
    }
  }
}
