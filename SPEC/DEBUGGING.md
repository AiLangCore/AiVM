# AiVM Debugging And Profiling

Status: moved.

The non-normative debug runtime/tooling direction now lives in:

- `../Design/Debugging.feature-debug-runtime.md`

The normative machine-readable debug artifact contract remains in:

- `DEBUG_ARTIFACTS.md`

This compatibility pointer remains so existing links do not fail. Do not add new
planning or design material here. Runtime/tooling proposals belong in
`../Design/`; active release tasks belong in `../Planning/`; stable artifact
contracts belong in `../SPEC/DEBUG_ARTIFACTS.md`.
